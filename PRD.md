# PRD: RFI Auto-Fill Multi-Agent System

> **Product:** RFI Agent v1  
> **Owner:** Avalere Health — ASK! RFI Team  
> **Date:** 2026-05-11  
> **Status:** Draft — pending decisions on orchestration framework, agent count, embedding model

---

## 1. Problem Statement

Avalere Health consultants manually fill RFIs (Requests for Information) from pharma/biotech clients. Each RFI is an Excel workbook with 50–200+ questions covering company info, compliance, data security, ESG, staffing, and more.

**Pain points:**
- 60–70% of questions are recurring across clients (same question, same answer)
- Consultants spend 4–8 hours per RFI on boilerplate answers they've written before
- No central system connects past answers to new questions
- Base info exists on SharePoint but isn't structured for quick retrieval
- Risk of inconsistent answers across RFIs for the same client

---

## 2. Goals

### v1 Goals (2-week sprint)
1. **Auto-fill boilerplate questions** — static company facts that never change
2. **Match and adapt past answers** — find similar questions from the 20 filled RFIs and reuse/adapt answers
3. **Flag gaps** — clearly mark questions the system couldn't confidently answer
4. **Preserve Excel formatting** — output is a new copy of the original Excel, not a stripped-down version

### Non-Goals (v1)
- Web UI / dashboard
- SharePoint API integration (blocked by InfoSec)
- .docx / .pptx format support
- Real-time collaboration
- Auto-submission to clients
- Fine-tuning any model

---

## 3. System Architecture

### High-Level Data Flow

```
┌────────────┐     ┌──────────────────┐     ┌────────────────┐
│   INPUT    │     │     AGENT 1      │     │    AGENT 2     │
│            │     │                  │     │                │
│  New RFI   │────▶│ Matcher + Filler │────▶│ Reviewer +     │
│  (.xlsx)   │     │                  │     │ Writer         │
└────────────┘     └──────────────────┘     └────────────────┘
                           │                        │
                  ┌────────▼────────┐      ┌───────▼────────┐
                  │ Azure AI Search │      │   Claude API   │
                  │   Dual Index    │      │   (Anthropic)  │
                  └─────────────────┘      └────────────────┘
```

> **Note:** The Parser is a **Python module** (not an LLM agent). It handles Excel I/O deterministically using openpyxl. The pipeline has **2 LLM agents** (Matcher+Filler, Reviewer) and **2 Python modules** (Parser, Writer). There is no separate Classifier agent — category inference is handled within the Matcher+Filler agent using sheet name hints and Azure AI Search's built-in filtering.

### Component Descriptions

#### Module: Parser (Python — no LLM)
- **Input:** Path to new RFI Excel file (.xlsx or .xlsm)
- **Output:** Structured list of questions with cell references
- **Logic:**
  - Open workbook with openpyxl (read-only mode for .xlsm to preserve macros)
  - Iterate all sheets; skip instruction-only sheets
  - Detect question/answer column structure (heuristic: look for "Question"/"Response"/"Answer" headers, or longest text column)
  - Handle merged cells by unmerging and attributing to first cell
  - Output: `List[{sheet_name, row, col, question_text, answer_cell_ref, existing_answer}]`

#### Agent 1: Matcher + Filler
- **Input:** List of extracted questions from Parser
- **Output:** Draft answers with confidence scores and traceable citations
- **Logic:**
  - **Category inference:** Use sheet name hints from Parser as category signal; no separate classification step. Azure AI Search handles semantic matching across all categories.
  - **Match (vector search):** For each question, query Azure AI Search Question Index → get top-3 similar past Q&A pairs with similarity scores
  - **Match (client):** Client name may be used as a soft signal but should NOT be over-indexed on — answers from other clients are equally valid if semantically relevant
  - **Boilerplate (similarity > 0.90):** Use past answer verbatim or near-verbatim
  - **Strong match (0.70–0.90):** Send to Claude: "Adapt this past answer to the new question"
  - **Weak match (0.50–0.70):** Send to Claude: "Using these references, draft an answer"
  - **No match (< 0.50):** Search Knowledge Index for base info → if found, Claude synthesizes; if not found → flag as empty
  - **Confidence scoring:** ALL confidence scores (including boilerplate) are generated by the LLM — no hardcoded values. The LLM assesses confidence based on source quality, relevance, and completeness.
  - **Citations:** Every answer MUST include traceable citations back to source documents (past RFI filename + question number, or base info document + section). Citations must be structured and machine-readable for auditability.
  - Output: `List[{question, answer, confidence, citations: [{source_file, section, question_id}]}]`

#### Agent 2: Reviewer + Writer
- **Input:** Draft answers with confidence scores
- **Output:** Filled Excel file + summary report
- **Logic:**
  - **Validate:** Check for contradictions (e.g., two answers give different employee counts)
  - **Write Excel:**
    - Copy original file to `{filename}_FILLED.xlsx`
    - Write answers into identified answer cells
    - Add "Confidence" column (or use next empty column)
    - Color-code answer cells: green (≥0.80), yellow (0.50–0.79), red (<0.50)
    - Preserve all original formatting, dropdowns, merged cells
  - **Summary report:** Generate `{filename}_SUMMARY.md` listing fill rate, flagged questions, sources used

---

## 4. Category Taxonomy

| # | Category | Base Info Source | Example Questions |
|---|---|---|---|
| 1 | Company Information | Company Information.pdf | Year founded, HQ location, ownership |
| 2 | Commercial Information | Commercial Information (General).pdf | Revenue, client list, case studies |
| 3 | Compliance | Compliance.html (to be converted) | Anti-bribery policy, audit history |
| 4 | Legal | *User to provide* | Contractual terms, liability, insurance |
| 5 | Data & Information Security | Data, information security....html | GDPR, SOC 2, data handling, encryption |
| 6 | ESG | Environmental, social, and governance.html | Sustainability, DEI, carbon reporting |
| 7 | People Information | People Information.html | Headcount, attrition, training |
| 8 | Suppliers & Freelancers | Suppliers and freelancers.html/.pdf | Subcontracting policy, vendor management |
| 9 | Technology & AI | Technology and AI.pdf | AI governance, tech stack, innovation |

---

## 5. Knowledge Base Strategy

See [knowledge-retrieval-strategy.md](./knowledge-retrieval-strategy.md) for full research.

**Summary:** Hybrid Vector RAG with Azure AI Search dual-index (Question Index + Knowledge Index), Azure-hosted embeddings, semantic ranker.

### Indexing Pipeline (one-time setup + incremental updates)
1. Parse all 20 filled RFIs → extract Q&A pairs → embed questions → store in Azure AI Search Question Index
2. Parse base info docs (PDF/HTML → text) → chunk by section → embed → store in Azure AI Search Knowledge Index
3. Extract metadata: category (from sheet name hint), source file, question ID for citation traceability
4. Client name stored as metadata but NOT used as a primary retrieval signal

---

## 6. Input / Output Specifications

### Input
- **New RFI file:** `.xlsx` or `.xlsm` file path
- **CLI command:** `rfi-agent fill <path-to-rfi.xlsx> [--client <name>] [--output-dir <path>]`

### Output
- **Filled Excel:** `{original_name}_FILLED.xlsx` — new copy with answers + confidence column + color-coding
- **Summary report:** `{original_name}_SUMMARY.md` — fill rate, flagged items, sources
- **Categorized workbook (optional):** If the input RFI isn't organized by category, produce a `{original_name}_CATEGORIZED.xlsx` with questions grouped into category sheets

### CLI Interface
```bash
# Fill a new RFI
rfi-agent fill "path/to/new_rfi.xlsx"

# Fill with explicit client name (improves matching)
rfi-agent fill "path/to/new_rfi.xlsx" --client "Pfizer"

# Re-index the knowledge base (after adding new past RFIs)
rfi-agent index

# Show stats about the knowledge base
rfi-agent stats
```

---

## 7. Edge Cases & Mitigations

| Edge Case | Mitigation |
|---|---|
| Merged cells in Excel | Unmerge, attribute content to top-left cell |
| Questions spanning multiple rows | Detect by: row has text but no answer column content, next row continues |
| Dropdown / validation cells | Preserve data validation in output; if answer must match a dropdown option, constrain generation |
| .xlsm macros | Open in read-only mode; copy file byte-for-byte, only modify answer cells via openpyxl |
| Very long questions (>512 tokens) | Truncate for embedding, but send full text to Claude |
| Ambiguous category | Assign primary + secondary category; search both in vector store |
| Same question worded differently | Handled by semantic similarity (embeddings catch paraphrases) |
| Empty / instruction-only sheets | Skip sheets with <3 question-like rows |
| Non-English content | Flag for human review; don't auto-fill |
| Password-protected Excel | Fail gracefully with clear error message |
| Contradictory past answers | Use most recent answer; flag discrepancy in summary |

---

## 8. v1 Scope Boundaries

### In Scope
- Excel parsing (.xlsx, .xlsm)
- 9-category classification
- Vector similarity matching against 20 past RFIs
- Base info retrieval (from parsed PDF/HTML)
- Boilerplate auto-fill
- Client-aware matching (filename-based)
- Confidence scoring + color-coded output
- CLI interface
- Summary report generation
- Web UI with live fill progress, per-answer character count display
- PDF / CSV / Excel download

### Out of Scope (v2+)
- SharePoint API integration
- .docx / .pptx parsing
- Feedback loop (consultant corrections → update vector store)
- Graph RAG / entity extraction
- Multi-language support
- Auto-updating base info
- Approval workflows
- Integration with document management systems

---

## 9. Open Decisions

| Decision | Options | Recommendation | Status |
|---|---|---|---|
| Orchestration framework | LangGraph / Pydantic AI / Plain Python | Plain Python (2-week constraint) | ⏳ Pending |
| Agent count | 4 consolidated / 7 granular | 4 agents | ⏳ Pending |
| Embedding model | Voyage AI / local sentence-transformers | Local (zero cost) | ⏳ Pending |

These decisions don't block implementation start — Phase 1 (parsing, indexing) is framework-agnostic.

---

## 10. Success Metrics (v1)

| Metric | Target |
|---|---|
| Fill rate (questions auto-answered) | ≥ 50% |
| Boilerplate accuracy | ≥ 95% |
| Adapted answer accuracy | ≥ 70% (consultant needs minor edits) |
| Processing time per RFI | < 5 minutes |
| Consultant time saved per RFI | ≥ 3 hours (from ~6h to ~3h) |

---

## 11. Risks

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Excel format variety breaks parser | High | Medium | Build robust heuristics; fall back to "flag all" |
| Low retrieval quality with 20 RFIs | Medium | High | Supplement with base info; tune similarity thresholds |
| Claude hallucination on compliance Qs | Low | High | Always ground in retrieved context; high flagging threshold |
| Base info docs are outdated | Medium | Medium | Timestamp sources; flag if base info is >6 months old |
| API cost surprises | Low | Low | Batch questions, cache responses, use local embeddings |
